<h1><a href="{{ $original_url }}">{{ $original_url }}</a> {{ $total_clicks + 1 }}</h1>
<p>While the app is in debug mode, redirects are not enabled. Disable <code>APP_DEBUG</code> for auto-redirect.</p>
